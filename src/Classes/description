Класс задания ()
	1.методы: 
		a)__construct($Подробности задания, $Категория, $Файлы = false, $Локация = false, $Бюджет  = false, $срок_исполнения = false)
		b)состояние(),
		с)Изменить статус($newStatus)
		d)Публикация_задания()
		e)Добавление_исполнителя($applicant)
		f)Завершить задание()
		g)Отказаться от задания($applicant)
		h)Начать выполнение()
	2. Свойтсва:
		private	$title;
		private $descriotion;
		private	$authorId;
		private $categoryId;
		private $taskPerformerId;
 
 
Класс статусов(переходы)
	1. Методы
		а)Карта Переходов()
		b)Получить следующий($currentStatus)
	2. Cвойства:
		public	$currentStatus;


Класс редактирования профиля 
	1. Методы
	__construct($userId)
		а)Получить информацию по юзеру()
		b)Обновление полей() //имя, email ....
	2. Cвойства:
		private	$userId;

	
Класс адресов (валидация, работа с координатами)
	1.Методы
		а)Валидация($adress)
		б)работа с координатами(отвалидированные адреса)

Класс откликов (отбор претендентов, валидация ) 
	1.Методы
		a)Добавить отклик($taskId)
